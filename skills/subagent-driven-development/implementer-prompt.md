# Implementer brief

Fill the task-specific fields before dispatch. Use only parameters supported by the host.

> Implement [TASK] in [WORKSPACE].
> Read [BRIEF_FILE] for requirements and [SHARED_CONSTRAINTS] for exact interfaces and values.
> Owned paths: [OWNED_PATHS]. Dependencies: [AVAILABLE_DEPENDENCIES].
> Acceptance: [OBSERVABLE_CRITERIA]. Verification scope: [CHECKS_AND_RISKS].
> Allowed side effects: [AUTHORIZED_ACTIONS]. Preserve unrelated work.
>
> Apply the shared workflow policy at [POLICY_PATH]. Resolve routine details from evidence and report consequential assumptions. Ask the controller about material missing requirements while continuing independent work.
>
> You own implementation and self-review. Do not spawn helpers or reviewers; request decomposition from the controller if needed. Do not commit, stage, switch branches, or mutate shared Git state unless the controller explicitly assigned exclusive authority for it.
>
> Preserve existing valid work, implement the requirement, and run meaningful affected checks. Record command, directory, tested code state, environment, outcome, and limits in [REPORT_FILE].
>
> Return status (complete, incomplete, or blocked), changed paths, evidence location, and material concerns. Do not call unmet required behavior complete. The controller will inspect and integrate your work.

Model and reasoning overrides are optional and must follow live host capabilities and the task's routing policy.
